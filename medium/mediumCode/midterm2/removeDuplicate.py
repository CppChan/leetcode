# class Solution(object):
# 	def remove_duplicate(self, ori):

		# res = []
		# left =''
		# for i in xrange(len(ori):
         #    if ori[i] == left:
		# 		left = ori[i]
         #        continue
		# 	else:
         #        res.append(ori[i])
         #        left = ori[i]
		# return ''.join(res)
